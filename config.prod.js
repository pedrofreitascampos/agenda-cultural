// Production Firebase config (committed to repo).
// Client-side Firebase API keys are safe to expose —
// security is enforced via Firebase Security Rules.

window.__APP_CONFIG = {
  firebase: {
    apiKey: "",
    authDomain: "",
    databaseURL: "",
    projectId: "",
    storageBucket: "",
    messagingSenderId: "",
    appId: "",
  },
  allowedEmails: [],
};
